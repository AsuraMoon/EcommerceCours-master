
{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
    <h1> Articles </h1><hr/>
    <div class="row">
        <div class="col-md4 mb-5">
            <div class="myCard" style="width:18rem;">
                <img class="" src="/image/{{product.illustration}}" alt="{{product.name}}">
                <div class="">
                    <h5 class="">{{product.name}}</h5>
                    <p class="">{{product.subtitle}}</p>
                    <span class="d-flex justify-content-between">
                        <a href="{{ path("add_cart", {"id": product.id}) }}" class="btn btn-secondary"> Ajouter au panier</a>
                        <p>{{ (product.price / 100) | number_format(2,',','.')}}¥</p>
                    </span>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
